{
  "name": "ai-flow-cli",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "skill-migration"
  ],
  "scripts": {
    "skill-migration": "npm run start --workspace=skill-migration",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
